---
phase: 05-api-ingestion
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - particle-flat-observatory/src/observatory/api_client.py
  - particle-flat-observatory/tests/test_api_client.py
autonomous: true

must_haves:
  truths:
    - "ParticleAPIClient raises ValueError listing missing env vars when PARTICLE_CLIENT_ID, PARTICLE_CLIENT_SECRET, or PARTICLE_SCOPE_ID are unset"
    - "ParticleAPIClient.get_flat_data(patient_id) returns dict[str, list[dict]] from Particle GET Flat endpoint"
    - "Auth endpoint is called with GET method, custom headers (client-id, client-secret, scope), and accept: text/plain"
    - "Requests retry automatically with exponential backoff + jitter on 429 and 5xx responses, respecting Retry-After header"
    - "Token is refreshed proactively when within 10 minutes of JWT expiry"
    - "401 during data fetch triggers one token refresh attempt before failing"
    - "Timeout and max_retries are configurable via PARTICLE_TIMEOUT and PARTICLE_MAX_RETRIES env vars"
  artifacts:
    - path: "particle-flat-observatory/src/observatory/api_client.py"
      provides: "ParticleAPIClient class with auth, retry, and get_flat_data"
      contains: "class ParticleAPIClient"
    - path: "particle-flat-observatory/tests/test_api_client.py"
      provides: "Unit tests for API client: credential validation, JWT decode, backoff calculation, retry behavior"
      contains: "test_"
  key_links:
    - from: "api_client.py"
      to: "urllib.request"
      via: "stdlib HTTP calls"
      pattern: "urllib\\.request\\.(Request|urlopen)"
    - from: "api_client.py"
      to: "base64 + json"
      via: "JWT payload decode for exp claim"
      pattern: "base64\\.urlsafe_b64decode"
---

<objective>
Create the Particle Health API client module with authentication, retry with exponential backoff, JWT token management, and the get_flat_data() method that returns the same dict shape as load_flat_data().

Purpose: This is the core HTTP layer for Phase 5 API ingestion -- it handles all Particle API communication (auth + data retrieval) with production-grade error handling (retries, timeouts, actionable errors). Plan 05-02 will wire this into the CLI.

Output: `api_client.py` module with ParticleAPIClient class, plus comprehensive unit tests covering credential validation, JWT decode, backoff math, and retry behavior.
</objective>

<execution_context>
@/Users/sangyetsakorshika/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangyetsakorshika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-ingestion/05-RESEARCH.md

# Existing modules for pattern reference (import style, logging, conventions)
@particle-flat-observatory/src/observatory/config.py
@particle-flat-observatory/src/observatory/parser.py
@particle-flat-observatory/src/observatory/normalizer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for ParticleAPIClient</name>
  <files>particle-flat-observatory/tests/test_api_client.py</files>
  <action>
Create test_api_client.py with unit tests covering:

1. **Credential validation tests:**
   - `test_missing_client_id_raises_value_error` -- env has no PARTICLE_CLIENT_ID, constructor raises ValueError mentioning "PARTICLE_CLIENT_ID"
   - `test_missing_client_secret_raises_value_error` -- same for PARTICLE_CLIENT_SECRET
   - `test_missing_scope_id_raises_value_error` -- same for PARTICLE_SCOPE_ID
   - `test_missing_multiple_vars_lists_all` -- all three missing, error message contains all three names
   - `test_valid_credentials_constructs_ok` -- all three set, no exception

2. **JWT decode tests:**
   - `test_decode_jwt_expiry_valid_token` -- construct a fake JWT with known exp claim (use base64url encoding), verify _decode_jwt_expiry returns correct datetime
   - `test_decode_jwt_expiry_no_exp_claim` -- JWT payload has no "exp" key, returns None
   - `test_decode_jwt_expiry_malformed_token` -- not a 3-part dotted string, returns None
   - `test_decode_jwt_expiry_invalid_base64` -- middle segment is not valid base64, returns None

3. **Backoff calculation tests:**
   - `test_backoff_attempt_0` -- first attempt backoff is between 1.0 and 2.0 (base 1.0 + jitter 0-1.0)
   - `test_backoff_attempt_2` -- third attempt backoff is between 4.0 and 5.0 (base 4.0 + jitter)
   - `test_backoff_respects_max` -- attempt 10 backoff is capped at 60.0 + jitter (not 1024.0)
   - `test_backoff_respects_retry_after_header` -- when 429 with Retry-After: 30, backoff is at least 30.0

4. **Configuration defaults tests:**
   - `test_default_base_url` -- defaults to "https://sandbox.particlehealth.com"
   - `test_default_timeout` -- defaults to 30.0
   - `test_default_max_retries` -- defaults to 3
   - `test_custom_timeout_from_env` -- PARTICLE_TIMEOUT=60 produces timeout of 60.0
   - `test_custom_max_retries_from_env` -- PARTICLE_MAX_RETRIES=5 produces max_retries of 5

Use `unittest.mock.patch.dict(os.environ, ...)` for env var manipulation. Import `_decode_jwt_expiry` as a module-level function (it should be importable). For backoff tests, mock `random.uniform` to return a fixed value (e.g., 0.5) so tests are deterministic.

Run tests -- they must all FAIL (RED phase) because api_client.py does not exist yet.
  </action>
  <verify>
`cd particle-flat-observatory && python -m pytest tests/test_api_client.py -v` shows all tests collected and all FAIL with ImportError (module does not exist).
  </verify>
  <done>Test file exists with 14+ test functions covering credential validation, JWT decode, backoff math, and configuration defaults. All tests fail because the module under test does not exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ParticleAPIClient to pass all tests</name>
  <files>particle-flat-observatory/src/observatory/api_client.py</files>
  <action>
Create api_client.py implementing the ParticleAPIClient class using ONLY stdlib (urllib.request, json, base64, time, random, logging, os). Follow the patterns from 05-RESEARCH.md code examples closely.

**Module structure:**

```
RETRYABLE_STATUS_CODES = {429, 500, 502, 503, 504}

def _decode_jwt_expiry(token: str) -> datetime | None:
    # base64url decode JWT payload, extract exp claim
    # return datetime.fromtimestamp(exp, tz=timezone.utc) or None

class ParticleAPIClient:
    def __init__(self) -> None:
        # Read PARTICLE_CLIENT_ID, PARTICLE_CLIENT_SECRET, PARTICLE_SCOPE_ID from env (required)
        # Read PARTICLE_BASE_URL (default: https://sandbox.particlehealth.com)
        # Read PARTICLE_TIMEOUT (default: 30), PARTICLE_MAX_RETRIES (default: 3)
        # Validate required vars, raise ValueError listing all missing

    def get_flat_data(self, patient_id: str) -> dict[str, list[dict]]:
        # Ensure token, call GET /api/v2/patients/{patient_id}/flat
        # Parse JSON response, validate it's a dict, return it

    def _ensure_token(self) -> None:
        # If no token or within refresh buffer of expiry, acquire new token

    def _acquire_token(self) -> None:
        # GET {base_url}/auth with client-id, client-secret, scope headers
        # Accept: text/plain -- response is plain text JWT, NOT JSON
        # On 401/403: raise ValueError with actionable fix steps (not retry)
        # Store token and decoded expiry

    def _request_with_retry(self, url: str, headers: dict[str, str]) -> bytes:
        # Retry loop: max_retries + 1 attempts
        # On 401: refresh token once, update Authorization header, continue
        # On 429/5xx: calculate backoff, respect Retry-After header, sleep, retry
        # On URLError/TimeoutError: backoff and retry
        # Log: endpoint, status, latency, retry attempts

    def _calculate_backoff(self, attempt: int, http_error=None) -> float:
        # min(1.0 * 2^attempt, 60.0) + random.uniform(0, 1.0)
        # If http_error is 429 with Retry-After, use max(calculated, retry_after)
```

**Key implementation details:**
- Never log client_secret or token values
- Log request URL, HTTP status, latency, retry attempt numbers
- Auth endpoint: method=GET, accept=text/plain, response is raw JWT string
- Data endpoint: method=GET, accept=application/json, response is JSON dict
- Refresh buffer: 600 seconds (timedelta(seconds=600))
- _request_with_retry handles 401 by refreshing token once (not infinite loop)
- urllib.request.Request objects are NOT reusable after header modification -- create new Request per retry attempt or use add_header/remove_header carefully

Run tests -- they must all PASS (GREEN phase).
  </action>
  <verify>
`cd particle-flat-observatory && python -m pytest tests/test_api_client.py -v` shows all tests passing. Zero failures.
  </verify>
  <done>api_client.py exists with ParticleAPIClient class. All 14+ tests pass. Module uses only stdlib imports (no httpx, no tenacity, no pydantic). Credential validation produces actionable error messages. JWT decode extracts exp claim via base64. Backoff formula is correct with jitter and Retry-After support.</done>
</task>

</tasks>

<verification>
1. `cd particle-flat-observatory && python -m pytest tests/test_api_client.py -v` -- all tests pass
2. `cd particle-flat-observatory && python -c "from observatory.api_client import ParticleAPIClient, _decode_jwt_expiry"` -- module is importable
3. `cd particle-flat-observatory && python -c "import observatory.api_client; import inspect; src = inspect.getsource(observatory.api_client); assert 'import httpx' not in src; assert 'import tenacity' not in src; assert 'import jwt' not in src; print('stdlib-only: OK')"` -- no forbidden dependencies
4. `cd particle-flat-observatory && python -m pytest tests/ -v` -- all existing tests still pass (no regressions)
</verification>

<success_criteria>
- ParticleAPIClient class exists in api_client.py with __init__, get_flat_data, _ensure_token, _acquire_token, _request_with_retry, _calculate_backoff
- _decode_jwt_expiry is a module-level function (importable separately for testing)
- All unit tests pass covering: credential validation, JWT decode, backoff calculation, configuration defaults
- Module uses ONLY stdlib (urllib.request, json, base64, time, random, logging, os, datetime)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-ingestion/05-01-SUMMARY.md`
</output>
