# Particle Health Management API credentials
# Get these from your Particle Health representative
PARTICLE_CLIENT_ID=your-client-id-here
PARTICLE_CLIENT_SECRET=your-client-secret-here

# Environment: "sandbox" or "production" (default: sandbox)
PARTICLE_ENV=sandbox

# Optional overrides (defaults are derived from PARTICLE_ENV)
# PARTICLE_TIMEOUT=30
