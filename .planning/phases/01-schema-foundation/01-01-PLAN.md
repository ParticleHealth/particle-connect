---
phase: 01-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - particle-flat-observatory/pyproject.toml
  - particle-flat-observatory/src/observatory/__init__.py
  - particle-flat-observatory/src/observatory/config.py
  - particle-flat-observatory/.env.example
  - particle-flat-observatory/sample-data/flat_data.json
  - particle-flat-observatory/.gitignore
autonomous: true

must_haves:
  truths:
    - "Project directory particle-flat-observatory exists at repo root with a valid Python package"
    - "pip install -e . succeeds from the project directory"
    - ".env.example documents all configuration variables with descriptions"
    - "Sample flat_data.json is present and contains all 21 resource types"
  artifacts:
    - path: "particle-flat-observatory/pyproject.toml"
      provides: "Python package definition with build config and dev dependencies"
      contains: "particle-flat-observatory"
    - path: "particle-flat-observatory/src/observatory/__init__.py"
      provides: "Package entry point"
    - path: "particle-flat-observatory/src/observatory/config.py"
      provides: "Configuration via environment variables"
      contains: "class ObservatorySettings"
    - path: "particle-flat-observatory/.env.example"
      provides: "All configuration variables documented"
      contains: "FLAT_DATA_PATH"
    - path: "particle-flat-observatory/sample-data/flat_data.json"
      provides: "Sample data for immediate testing"
  key_links:
    - from: "particle-flat-observatory/src/observatory/config.py"
      to: ".env.example"
      via: "Environment variable names match between config class and .env.example"
      pattern: "FLAT_DATA_PATH|DDL_DIALECT|OUTPUT_DIR"
---

<objective>
Create the particle-flat-observatory project skeleton with Python package structure, configuration management, sample data, and developer setup so that subsequent plans have a working project to build within.

Purpose: Every subsequent plan needs a valid Python project with importable modules, configuration, and sample data. This is the foundation.
Output: A pip-installable Python package at particle-flat-observatory/ with config, sample data, and .env.example.
</objective>

<execution_context>
@/Users/sangyetsakorshika/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangyetsakorshika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-foundation/01-CONTEXT.md
@particle-health-starters/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project skeleton and Python package</name>
  <files>
    particle-flat-observatory/pyproject.toml
    particle-flat-observatory/src/observatory/__init__.py
    particle-flat-observatory/.gitignore
  </files>
  <action>
Create the directory `particle-flat-observatory/` at the repo root.

Create `pyproject.toml` with:
- name: "particle-flat-observatory"
- version: "0.1.0"
- description: "Data pipeline accelerator for Particle Health flat data — schema generation, normalization, and analytics"
- requires-python: ">=3.11"
- dependencies: [] (no external deps needed — this phase uses only stdlib: json, logging, pathlib, argparse)
- optional dev dependencies: pytest>=8.0.0, ruff>=0.1.0
- build-system: hatchling (matching existing particle-health-starters convention)
- [tool.hatch.build.targets.wheel] packages = ["src/observatory"]
- [tool.ruff] line-length = 100, target-version = "py311"
- [tool.ruff.lint] select = ["E", "F", "I", "W"]
- [tool.pytest.ini_options] testpaths = ["tests"], pythonpath = ["src"]

Create `src/observatory/__init__.py` with:
- __version__ = "0.1.0"
- Brief module docstring: "Particle Health flat data observatory — schema generation, normalization, and analytics."

Create `.gitignore` with standard Python ignores:
- __pycache__/, *.pyc, *.pyo
- .venv/, venv/, env/
- *.egg-info/, dist/, build/
- .env (NOT .env.example)
- .ruff_cache/
- .pytest_cache/
- ddl/postgres/create_all.sql and ddl/bigquery/create_all.sql (generated outputs — will be committed explicitly after generation)

Do NOT add external dependencies like structlog or pydantic. This project uses only Python stdlib for maximum portability (customers may not want extra deps for a DDL generator).
  </action>
  <verify>
Run from repo root:
1. `ls particle-flat-observatory/pyproject.toml` — file exists
2. `ls particle-flat-observatory/src/observatory/__init__.py` — file exists
3. `cd particle-flat-observatory && python -m py_compile src/observatory/__init__.py` — compiles without error
4. `cd particle-flat-observatory && pip install -e ".[dev]" --dry-run` — resolves dependencies without error
  </verify>
  <done>
particle-flat-observatory/ directory exists with a valid, pip-installable Python package structure matching the conventions of the existing particle-health-starters package.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create configuration module and .env.example with sample data</name>
  <files>
    particle-flat-observatory/src/observatory/config.py
    particle-flat-observatory/.env.example
    particle-flat-observatory/sample-data/flat_data.json
  </files>
  <action>
Create `src/observatory/config.py` with:
- A dataclass or simple class `ObservatorySettings` that reads configuration from environment variables (use os.environ.get with defaults — do NOT use pydantic-settings, keep it stdlib-only).
- Configuration variables:
  - FLAT_DATA_PATH: str — path to flat_data.json (default: "sample-data/flat_data.json")
  - DDL_DIALECT: str — "postgres" or "bigquery" (default: "postgres")
  - OUTPUT_DIR: str — directory for generated DDL files (default: "ddl")
  - LOG_LEVEL: str — logging level (default: "INFO")
- A `load_settings()` function that:
  - Reads from os.environ
  - Supports .env files by checking if python-dotenv is available and loading it (optional dependency, not required)
  - Returns an ObservatorySettings instance
  - Validates DDL_DIALECT is one of "postgres" or "bigquery"
  - Raises ValueError with actionable message if invalid dialect provided

Create `.env.example` with all variables documented:
```
# Path to Particle flat data JSON file
FLAT_DATA_PATH=sample-data/flat_data.json

# Database dialect for DDL generation: "postgres" or "bigquery"
DDL_DIALECT=postgres

# Output directory for generated DDL files
OUTPUT_DIR=ddl

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
```

Copy the sample data file:
- Source: `particle-health-starters/sample-data/flat_data.json`
- Destination: `particle-flat-observatory/sample-data/flat_data.json`
- This is a copy, not a symlink — the observatory project should be self-contained.

Update `src/observatory/__init__.py` to export ObservatorySettings and load_settings from config module.
  </action>
  <verify>
Run from particle-flat-observatory/:
1. `python -c "from observatory.config import load_settings; s = load_settings(); print(s.flat_data_path, s.ddl_dialect)"` — prints "sample-data/flat_data.json postgres"
2. `python -c "import json; data = json.load(open('sample-data/flat_data.json')); print(len(data.keys()))"` — prints "21"
3. `cat .env.example` — shows all 4 variables with comments
4. `python -c "import os; os.environ['DDL_DIALECT']='invalid'; from observatory.config import load_settings; load_settings()"` — raises ValueError with actionable message
  </verify>
  <done>
Configuration module reads environment variables with sensible defaults, .env.example documents all variables, and sample flat_data.json with all 21 resource types is present for immediate testing. The project is self-contained — no references to files outside particle-flat-observatory/.
  </done>
</task>

</tasks>

<verification>
From the repo root:
1. `ls particle-flat-observatory/pyproject.toml particle-flat-observatory/src/observatory/__init__.py particle-flat-observatory/src/observatory/config.py particle-flat-observatory/.env.example particle-flat-observatory/sample-data/flat_data.json` — all files exist
2. `cd particle-flat-observatory && pip install -e . && python -c "from observatory.config import load_settings; s = load_settings(); print('OK:', s.ddl_dialect)"` — prints "OK: postgres"
3. `python -c "import json; d = json.load(open('particle-flat-observatory/sample-data/flat_data.json')); assert len(d) == 21, f'Expected 21, got {len(d)}'; print('21 resource types confirmed')"` — passes
</verification>

<success_criteria>
- particle-flat-observatory/ is a valid Python package that installs with pip
- Configuration reads from environment with documented defaults
- .env.example has all 4 configuration variables with descriptions
- Sample flat_data.json contains all 21 Particle resource types
- No external dependencies required (stdlib only)
- Conventions match existing particle-health-starters (ruff config, pytest config, hatchling build)
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-foundation/01-01-SUMMARY.md`
</output>
