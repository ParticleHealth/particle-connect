---
phase: 02-local-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - particle-flat-observatory/compose.yaml
  - particle-flat-observatory/pyproject.toml
  - particle-flat-observatory/.env.example
autonomous: true

must_haves:
  truths:
    - "docker compose up starts PostgreSQL container and creates all 21 tables automatically (16 with columns + 5 commented placeholders)"
    - "docker compose down and docker compose up preserves loaded data (volume persistence)"
    - "PG_PORT env var changes which host port maps to container port 5432"
    - "pip install -e '.[pipeline]' installs psycopg, typer, and python-dotenv"
  artifacts:
    - path: "particle-flat-observatory/compose.yaml"
      provides: "PostgreSQL service with auto-DDL, healthcheck, named volume"
      contains: "docker-entrypoint-initdb.d"
    - path: "particle-flat-observatory/pyproject.toml"
      provides: "Pipeline dependencies and CLI entry point"
      contains: "psycopg"
    - path: "particle-flat-observatory/.env.example"
      provides: "All PostgreSQL connection variables documented"
      contains: "PG_HOST"
  key_links:
    - from: "particle-flat-observatory/compose.yaml"
      to: "particle-flat-observatory/ddl/postgres/create_all.sql"
      via: "volume mount into /docker-entrypoint-initdb.d/"
      pattern: "create_all\\.sql.*docker-entrypoint-initdb"
---

<objective>
Create Docker Compose infrastructure for local PostgreSQL with auto-DDL table creation, update pyproject.toml with Phase 2 dependencies and CLI entry point, and extend .env.example with PostgreSQL connection variables.

Purpose: Establish the infrastructure foundation that all subsequent Phase 2 plans build on -- a running PostgreSQL database with tables ready to receive data, and Python dependencies installed for the loader and CLI.

Output: compose.yaml, updated pyproject.toml, updated .env.example
</objective>

<execution_context>
@/Users/sangyetsakorshika/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sangyetsakorshika/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-local-pipeline/02-RESEARCH.md
@particle-flat-observatory/pyproject.toml
@particle-flat-observatory/.env.example
@particle-flat-observatory/ddl/postgres/create_all.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker Compose file with PostgreSQL service</name>
  <files>particle-flat-observatory/compose.yaml</files>
  <action>
Create `compose.yaml` (NOT `docker-compose.yml` -- Compose V2 uses this filename, no `version:` key) in the particle-flat-observatory/ directory with:

**Service: postgres**
- Image: `postgres:17-alpine`
- Container name: `observatory-postgres`
- Environment: POSTGRES_USER=observatory, POSTGRES_PASSWORD=observatory, POSTGRES_DB=observatory
- Ports: `"${PG_PORT:-5432}:5432"` (configurable host port, always container port 5432)
- Volumes:
  - Named volume `pgdata:/var/lib/postgresql/data` (data persistence)
  - Read-only bind mount: `./ddl/postgres/create_all.sql:/docker-entrypoint-initdb.d/01-create-tables.sql:ro` (auto-DDL on first startup)
- Healthcheck: `pg_isready -U observatory -d observatory` with interval=5s, timeout=3s, retries=5, start_period=10s
- Restart policy: `unless-stopped`

**Top-level volumes:**
- `pgdata:` (named volume declaration)

Do NOT add a `version:` key -- it is obsolete in Compose V2.
Do NOT create a separate docker/ directory or symlinks -- mount the existing DDL file directly.
  </action>
  <verify>
Run `cd /Users/sangyetsakorshika/Documents/GitHub/particle-connect/particle-flat-observatory && docker compose config` to validate the compose file syntax. Should output the resolved configuration without errors.
  </verify>
  <done>compose.yaml exists, passes syntax validation, mounts create_all.sql into init directory, uses configurable PG_PORT with 5432 default, has healthcheck and named volume for persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Update pyproject.toml with pipeline dependencies and CLI entry point</name>
  <files>particle-flat-observatory/pyproject.toml, particle-flat-observatory/.env.example</files>
  <action>
**Update pyproject.toml:**
1. Add `dependencies` array (replacing empty `[]`):
   ```
   dependencies = [
       "psycopg[binary]>=3.3.0",
       "typer>=0.21.0",
       "python-dotenv>=1.0.0",
   ]
   ```
   Note: python-dotenv moves from optional to required in Phase 2 because typer CLI needs reliable .env loading for all users, not just those who happen to have it installed.

2. Add new CLI entry point alongside existing one:
   ```
   [project.scripts]
   observatory-generate-ddl = "observatory.generate_ddl:main"
   particle-pipeline = "observatory.cli:app"
   ```

3. Keep all existing config (build-system, optional-dependencies, tool sections) unchanged.

**Update .env.example:**
Add PostgreSQL connection variables BELOW the existing Phase 1 variables (keep existing lines unchanged):
```
# PostgreSQL connection (for local Docker pipeline)
PG_HOST=localhost
PG_PORT=5432
PG_USER=observatory
PG_PASSWORD=observatory
PG_DATABASE=observatory
```

These defaults match the compose.yaml service so the pipeline works out of the box with `docker compose up`.
  </action>
  <verify>
Run `cd /Users/sangyetsakorshika/Documents/GitHub/particle-connect/particle-flat-observatory && pip install -e ".[dev]"` (using the dev machine's Python). Verify psycopg and typer are installed: `python -c "import psycopg; import typer; print('OK')"`. Run existing tests: `python -m pytest tests/ -q` -- all 86+ tests must still pass.
  </verify>
  <done>pyproject.toml has psycopg[binary], typer, python-dotenv as dependencies; particle-pipeline entry point declared; .env.example has all PG_* variables documented; all existing tests still pass.</done>
</task>

</tasks>

<verification>
1. `docker compose config` in particle-flat-observatory/ succeeds without errors
2. `pip install -e ".[dev]"` installs all dependencies including psycopg, typer
3. `python -c "import psycopg; import typer; print('OK')"` prints OK
4. All existing tests pass: `python -m pytest tests/ -q`
5. .env.example contains all PG_* variables with matching defaults to compose.yaml
</verification>

<success_criteria>
- compose.yaml creates PostgreSQL 17 container with auto-DDL via init script mount
- PG_PORT is configurable via environment variable (default 5432)
- Data persists via named volume pgdata
- Pipeline Python dependencies installable via pip
- particle-pipeline CLI entry point registered (will be wired in Plan 02-03)
- No regressions in existing Phase 1 tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-pipeline/02-01-SUMMARY.md`
</output>
